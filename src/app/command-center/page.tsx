import { desc, eq } from "drizzle-orm"; import { FeatureShell } from "@/components/feature-shell"; import { db } from "@/lib/db/client"; import { alerts } from "@/lib/db/schema"; import { getActiveProjectId } from "@/lib/projects/current"; import { getDashboardData, resolveAlertLinks } from "@/lib/dashboard-data";
export const dynamic = "force-dynamic";
export default async function CommandCenterPage() { const projectId = await getActiveProjectId(); const [data, items] = await Promise.all([getDashboardData(projectId), db.select().from(alerts).where(eq(alerts.projectId, projectId)).orderBy(desc(alerts.createdAt))]); if (!data) throw new Error("Project not found"); const resolved = await resolveAlertLinks(projectId, items); return <FeatureShell projectName={data.project} eyebrow="Unified event view" title="Command center" description="Read-only operational alerts cross-link agent events to project impacts without changing readiness or schedule dates."><div className="workflow-stack">{resolved.map(({ alert, links }) => <article className="surface workflow-card" key={alert.id}><span className={`source-status ${alert.status === "active" ? "pending" : "processed"}`}>{alert.status}</span><h2>{alert.title}</h2><p>{alert.eventType.replaceAll("_", " ")} · {new Intl.DateTimeFormat("en-IN", { dateStyle: "medium", timeStyle: "short" }).format(alert.createdAt)}</p>{links.length > 0 && <nav className="alert-links">{links.map((link) => <a className="button button-secondary" href={link.href} key={link.href}>{link.label}</a>)}</nav>}<small className="mono">{JSON.stringify(alert.payload)}</small></article>)}</div></FeatureShell>; }
