import { FeatureShell } from "@/components/feature-shell"; import { KnowledgeSearch } from "@/components/knowledge-search"; import { getActiveProjectId } from "@/lib/projects/current"; import { getDashboardData } from "@/lib/dashboard-data";
export const dynamic = "force-dynamic";
export default async function KnowledgePage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const projectId = await getActiveProjectId(); const data = await getDashboardData(projectId); const { q = "" } = await searchParams; if (!data) throw new Error("Project not found"); return <FeatureShell projectName={data.project} eyebrow="Project-scoped retrieval" title="Knowledge" description="Searches controlled project source regions and refuses to return uncited claims."><KnowledgeSearch projectId={projectId} initialQuery={q.slice(0, 500)} /></FeatureShell>; }
