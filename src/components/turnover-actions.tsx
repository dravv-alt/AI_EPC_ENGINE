"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function TurnoverActions({ projectId, approvedGates }: { projectId: string; approvedGates: Array<{ id: string; name: string }> }) { const router = useRouter(); const [gateId, setGateId] = useState(approvedGates[0]?.id ?? ""); const [message, setMessage] = useState(""); async function generate() { const response = await fetch(`/api/projects/${projectId}/turnover-packs`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ gateId }) }); const body = await response.json(); setMessage(response.ok ? `Verified turnover pack ${body.pack.manifestHash.slice(0, 12)}… created.` : body.error); if (response.ok) router.refresh(); } return <section className="surface workflow-form"><label>Approved gate<select value={gateId} onChange={(event) => setGateId(event.target.value)}>{approvedGates.map((gate) => <option value={gate.id} key={gate.id}>{gate.name}</option>)}</select></label><button className="button button-primary" onClick={generate} disabled={!gateId}>Generate immutable pack</button>{message && <p className="form-message">{message}</p>}</section>; }
